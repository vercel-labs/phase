*.tsbuildinfo
.cache
.claude
.cursor
!.cursor/commands
!.cursor/skills
.deepsec
.env
.env*.local
.logs
.planning
.turbo
.vercel
node_modules/
# Anchored to the repo root so it only ignores the build output — not the
# packaged skill at skills/phase/dist/, which is committed for direct download.
/dist/
public/
