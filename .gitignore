*.tsbuildinfo
.cache
.claude
.cursor
!.cursor/commands
!.cursor/skills
.deepsec
.env
.env*.local
.logs
.planning
.turbo
.vercel
node_modules/
# Anchored to the package so it ignores library build output, not the packaged
# skill at skills/phase/dist/, which is committed for direct download.
/packages/phase/dist/
public/
