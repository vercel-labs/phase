Audit the blog article route in `workspace/app/blog/[slug]` and every component
the article can render for browser runtime performance. Return a report that
engineers and product partners can use to decide what to fix first.
